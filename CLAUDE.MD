\# Project scope



This repository root is the only project workspace.



Rules:

\- Do not inspect or use files outside this repository unless I explicitly ask.

\- Do not search Downloads, Documents, Desktop, or old/deleted projects.

\- Treat docs/assignment.pdf as the source of truth for requirements.

\- If the repository is empty, scaffold the project here instead of searching for another codebase.

\- Do not launch parallel agents unless I explicitly ask.

\- Do not perform broad repository exploration when the repo is small.

\- Keep token usage low.

\- Prefer compact summaries over long analysis.

\- For this project, use PHP + vanilla JavaScript.

\- Prefer a classic PHP multi-page app with lightweight JSON endpoints.

\- Keep business rules authoritative on the server.

